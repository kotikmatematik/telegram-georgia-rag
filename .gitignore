# Secrets and data — NEVER commit (chats contain people's personal data)
.env
data/
chroma_db/
*.session
*.session-journal

# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
.python-version
# uv.lock is intentionally committed for reproducible installs
